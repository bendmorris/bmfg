# BMFG: BitMap Font Generator

Creates BMFont-compatible bitmap fonts with optional premultiplied alpha and 
text borders, using PyGame and FreeType.

To install:

    python setup.py install

For usage info:

    python -m bmfg -h
